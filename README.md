# Token Guardian
Token Guardian is a predictive cost-benefit analysis tool that flags when AI usage exceeds projected runway.

## Usage
1. Create a JSON file containing AI usage data.
2. Run the `token_guardian.py` script.
3. The script will output predictive insights and cost-saving measures.

## Requirements
* Python 3.8+
* No external dependencies required.
